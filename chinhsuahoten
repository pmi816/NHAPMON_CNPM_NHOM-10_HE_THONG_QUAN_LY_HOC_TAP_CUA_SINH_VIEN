def update_full_name(user_id, new_full_name, users):
    for user in users:
        if user["id"] == user_id:
            user["full_name"] = new_full_name
            print("✅ Cập nhật họ tên thành công!")
            return True

    print("❌ Không tìm thấy người dùng!")
    return False
